# Antigravity 開発ガイドライン & プロジェクト共通ルール

このファイルは、本プロジェクトおよびすべての開発作業においてエージェントが最優先で遵守すべき全体開発ルールです。

---

## 1. 相談モード（Consultation Mode）の厳格運用
ユーザーのプロンプトが「相談です。」「相談です」またはそれに準ずる言葉で始まる場合、エージェントは以下のルールを厳格に遵守すること。

1. **自律的変更の完全停止**: 一切のコード修正・ファイル編集・コマンド実行等の実装作業を自律的に行わないこと。
2. **回答範囲の限定**: 回答はスレッド内での「分析・検証・助言・設計の整理」のみに限定すること。
3. **①②③形式の徹底**: 提案・論点・改善案は、必ず「①②③…」の丸数字による通し番号を付与して整理すること。
   - 目的: ユーザーが「①採用、②不採用」と手短かつ的確に即答・判断できるようにするため（ユーザーから都度指定がなくても自動的に①②③形式で出力すること）。
4. **明示的指示待ち**: ユーザーが納得し、明示的に「実装してください」「実行してください」等の指示を出すまで実装に着手しないこと。
5. **全体目的**: 構想を行き当たりばったりで実装せず、事前にユーザーと思考・道筋を整理することで手戻りを防ぎ、トークン消費を最小限に抑えて最高品質のアウトプットを実現するため。

---

## 2. Localhostモード ＆ システムログ開発規約（呪文「localhost設定をして」）
「localhost設定をして」の指示、またはローカル開発環境構築・システムログ機能追加時の最優先ルール：

1. **スクロール追従型・固定フロートボタン『localhost』の常時表示**:
   - `http://localhost:*` または `127.0.0.1` 接続時は、画面をスクロールしても常に視認・タップできる **固定フローティングボタン『localhost』**（`position: fixed; bottom: 24px; right: 24px; z-index: 99999;`）を常駐表示すること。
   - グリーンLEDパルス点滅（`pulse-dot`）とリアルタイムログ件数バッジ（`log-count-pill`）を完備すること。
   - このボタンを押すと、即座にリッチシステムログ・詳細プログラム診断コンソール（`#logModal`）が開くこと。
2. **ヘッダー上の重複システムログボタンの排除**:
   - 画面ヘッダー等にある重複した「シスログ」ボタンやログ確認タグは削除・非表示にし、本番UIをスッキリと洗練された状態に保つこと。
3. **クラウド本番環境での隠しアンロック（PIN: 1234）**:
   - 本番クラウド環境（Pages公開URL等）では初期状態で一般ユーザー向けに非表示とするが、ヘッダータイトルの **10回連続タップ＋PINコード（`1234`）** でクラウド上でも即座に『localhost』フロートボタンをアンロック・ログ監視可能にすること。

---

## 3. クラウド直結 ＆ データ保護同期規約（Direct Cloud Connect & Zero Data-Loss）
1. **Localhost ➔ 本番クラウド直結通信**:
   - ローカル開発環境（`localhost`）ではローカル静的サーバー（Python等）による「HTTP 404 エラー」を回避するため、同期先エンドポイントを自動的に本番クラウド（Cloudflare Pages等: `https://<domain>/api/sync`）へ直結切り替えすること。
2. **空データ上書き・消去の絶対防止（Zero Data Loss Guard）**:
   - Localhostの初期状態（データ0件）がクラウド本番データを上書き・消去してしまわないよう、初回起動時はクラウド受信（Pull）を先行させ、空データでの誤Push・自動上書き送信を厳格にブロックすること。
3. **大容量データ通信の最適化とアダプティブタイムアウト**:
   - 画像などの大容量アーカイブを含む場合、Base64のインテリジェント圧縮を行い通信サイズを削減すること。
   - データ容量に応じたアダプティブタイムアウト（30秒〜60秒）を設定し、通信中断（`signal is aborted without reason`）を永久に防止すること。

---

## 4. バージョン管理・キャッシュバスター同期規約（Version Integrity & Cache-Busting）
1. **キャッシュバスターの完全付与**:
   - `index.html` 内の全CSS/JS読み込みタグに、必ずバージョン連動のクエリパラメータを付与すること（例: `<link rel="stylesheet" href="style.css?v=X.X.X">` / `<script src="app.js?v=X.X.X"></script>`）。
   - ブラウザが古いバージョンのコードをキャッシュし続ける不具合を100%根絶すること。
2. **バージョン表記の三位一体（完全一致）**:
   - ソースコード先頭（コメントヘッダー）
   - UI上の表示箇所（タイトル横の「vX.X.X」バッジ）
   - システムログ初期化メッセージ（`Logger.info("... vX.X.X ...")`）
   - 起動スクリプト（`start.sh`）
   - `package.json`
   これら各所のバージョン表記を必ず完全に同期・一致させ、古いバージョン表記を残さないこと。
3. **報告時の明記**: 作業完了時のユーザー報告には、更新後のバージョン番号と変更内容を明記すること。

---

## 5. 大容量データストレージ規約（StorageVault - IndexedDB）
1. **LocalStorage容量制限（5MB）の回避**:
   - カード画像、透過レイヤー、アーカイブなどの大容量データは、5MBでクラッシュする LocalStorage ではなく、必ず **IndexedDB（数GB対応の `StorageVault`）** に格納・管理すること。
2. **APIキー等の安全な分離管理**:
   - ユーザーのAPIキーや認証情報は、アプリ更新でも絶対に消えない端末固定キー（`kuwagata_vault_*`）として永続金庫に安全に分離保管すること。

